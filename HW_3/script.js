let firstnumber = prompt('Enter the first number.');
let secondnumber = prompt('Enter the second number.')


alert(`Your result:
${Number(firstnumber)}+${Number(secondnumber)}=${Number(firstnumber) + Number(secondnumber)}
${Number(firstnumber)}-${Number(secondnumber)}=${Number(firstnumber) - Number(secondnumber)} 
${Number(firstnumber)}*${Number(secondnumber)}=${Number(firstnumber) * Number(secondnumber)}
${Number(firstnumber)}/${Number(secondnumber)}=${Number(firstnumber) / Number(secondnumber)}`);